# FAQ

- Can I edit events.jsonl by hand? Append, yes; rewrite, no — the seq must
  stay gapless or every derived view refuses to build.
- Where is the server? There isn't one. It is a CLI and two directories.
- Why "pantrylog"? A kitchen log is the smallest honest event-sourced app.
