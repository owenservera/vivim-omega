# FACT BASE 2026-09-16 — Code-Only Ground Truth (supersedes all prior docs)

**Rule for this file:** every claim below was read from disk on 2026-09-16. No prior
`migration-devops/` doc, no wiki, no handoff was used as a source. Paths are relative
to `Zai/V15/` unless stated. Stale plan in `00-ASSESSMENT/`, `10-WAVE0/`, `20-WAVES/`
is preserved as history but is **not** the plan anymore. This file + `STRATEGY-OMEGA-PLUGIN-REBUILD.md`
+ `10-WAVE0/WAVE0-NEEDS-FROM-CODE.md` are the plan now.

## 1. What was cloned, where, at what commit

| Repo | URL | Local path | HEAD (single-commit snapshot) |
|---|---|---|---|
| vivim-final-program | https://github.com/owenservera/vivim-final-program.git | `migration-devops/vivim-old-repos/vivim-final-program` | `4a5eb84` — "VIVIM upgrade program — Round-2 P0 complete (29/29, D-116)" |
| vivim-final-enhanced | https://github.com/owenservera/vivim-final-enhanced.git | `migration-devops/vivim-old-repos/vivim-final-enhanced` | `afebe00` — "enhanced working tree (kernel-plugin WIP)" |
| omega core (target) | local | `vivim-omega-turn-015-RECOVERED/clone-omega` | `61d1a41` — D-372 Phases 1-2 (working tree dirty: 21 modified files, D-372 in progress) |

Both legacy remotes expose **only `master`** — no branches, no tags. History is one
public snapshot each. There is nothing to diff across time; the only diff that matters
is program-vs-enhanced on disk, plus legacy-vs-omega architecture.

## 2. Legacy program: measured facts

**Identity:** `package.json` → `vivim-final@1.0.0`, "cap-store v1 Knowledge Graph Rebuild —
local-first AI conversation platform". ESM (`type: module`), `src/index.ts` entry,
`vivim` bin at `src/cli/index.ts`. Runtime: **Bun** (`bunfig.toml`, `bun.lock`,
`bun.test.config.ts`, `bun x prisma`, `bun run --cwd frontend`).

**Scale (disk counts):**
- `src/`: **954 files**, ~5.45 MB of `.ts` (measured `Get-ChildItem -Recurse src/*.ts`).
- `tests/`: **503 files** (`tests/{unit,integration,e2e,arch}` + `test:fast` lane).
- `seeds/`: **52 files** (`adapters, automation, capabilities, command-descriptions,
  conceptual-model, harness, intent-templates, parsers, providers, system, taxonomy, user`
  + `memory-intelligence.ts` + `og-capability-port.ts` + `seed-snapshot.db-shm/wal`).
- `src/engines/`: 30+ subdirs (`actor, adapters, automation, browser-automation,
  capability-bootstrap, chrome, code-audit, command-language, events, generative, harness,
  kernel, local-agent, local-server, memory, nlcl, observability, onboarding, opencode,
  p2p-node, parsers, pool, providers, reliability, reprogrammability, resource, runtime,
  scheduler, stealth, tunnel-client, tunnel-orchestrator, workflow-templates`) + ~190
  top-level engine files. Heaviest single files seen: `conversation-manager.ts` (~40 KB),
  `autonomous-execution.ts` (~44 KB), `chrome-governor.ts` (~30 KB),
  `provider-protocol-generator.ts` (~22 KB), `context-assembly.ts` (~23 KB).
- `src/server/`: **40+ routers** — `index.ts` alone ~49 KB, plus `conversation-router.ts`
  (~20 KB), `engines-catalog.ts` (~25 KB), `plugin-router.ts` (~24 KB),
  `module-registry.ts`, `service-container.ts` (DI), `websocket.ts`, `canvas-router.ts`,
  `memory-viz-router.ts`, etc. This is a **monolith HTTP+WS server**, not a set of services.
- `src/engines/stealth/`: **19 files** (`audio-context, behavioral-pattern, canvas-noise,
  cdp-artifact-cleaner, extension-bridge, font-screen, human-keyboard/mouse/scroll,
  launch-profile, navigator-patch, network-fingerprint, profile-warmup, stealth-module(+engine),
  stealth-profile-store, webgl-spoof` + `index` + `register-defaults`).
- `prisma/`: **dual DB** — `prisma/system/schema.prisma` (~77 KB, **111 `model` blocks**),
  `prisma/user/schema.prisma` (~66 KB, **90 `model` blocks**) = **201 models total**,
  zero `enum` blocks in either. Migrations in `prisma/{system,user}/migrations/` +
  legacy `prisma/migrations/` + `prisma/migrations.bak/` (2026-07/08). Scripts:
  `prisma:migrate:dev/prod`, `prisma:generate`, `prisma:push`, `db:doctor/backup/restore`.
- `frontend/`: **Next.js** (`vivim-frontend@0.2.0`, `next dev -p 3000`, `next build`,
  `TAURI_STATIC_EXPORT=1 next build`), Radix UI (~20 packages), TanStack
  (`react-query`, `react-virtual`), Tauri v2 (`@tauri-apps/*`, `src-tauri/` Rust shell
  with `Cargo.toml/lock`, `tauri.conf.json`, `capabilities/`), Prisma client, Playwright
  e2e (`playwright.config.ts`, `tests/e2e/`), own `prisma/`, `seeds/`, `mini-services/`,
  `plugins/`. This is a **full product frontend**, not a thin console.
- `src-tauri/`: Rust shell (`build.rs`, `Cargo.lock` 125 KB, `src/`, `capabilities/`, `data/`, `icons/`).
- `src/mcp/`: 11 files (`browser-mcp/session/tools`, `discovery-tools`, `nlcl-tools`,
  `serp-parser`, `server`, `types`, `zod-schema`).
- `src/cli/`: `index.ts` + `commands/` + `command-registry`, `discovery-stack`,
  `provider-harness`, `repl`, `pipeline-engine`.
- `src/storage/`: `contracts/`, `cozo/`, `impl/`, `migration/` + `db.ts` (~18 KB),
  `snapshot.ts`, `prisma.ts`, `store-factory.ts` — storage is Prisma + Cozo + snapshot mix.
- `seeds/providers/`: `manifests.ts` (~33 KB, AUTO-GENERATED from JSON) + 8 small JSON
  (`anthropic-api, discord, notion, openai-api, openrouter, reddit, slack, whatsapp`
  ~1.1–1.3 KB each). `manifests.ts` provider slugs seen: `chatgpt, claude, deepseek,
  facebook, gemini, generic, qwen, slack, studio-ai, system, telegram, whatsapp, z-ai`
  (+ model slugs like `gpt-4o/o3`, `claude-sonnet/opus/haiku`, `gemini-2.5-pro/flash`,
  `qwen-max/plus/turbo`, `deepseek-chat/reasoner`). Flow per
  `src/engines/provider-registrar.ts` header: manifests → DB seed (multi-table atomic
  inserts incl. capabilities/endpoints/models/parsers) → `provider-protocol-generator.ts`
  reads DB → static file. **Providers are DB-seeded singletons, not plugins.**
- `src/engines/plugin-system.ts` header: `ProviderPlugin` = in-process hooks
  (`onRegister/onResolveCapabilities/onAction/onProjectState/onParse` + Phase-9
  `surfaces/mutationHandlers/capabilities`), `PluginManagerImpl` backed by
  `CapabilityEventBus` (`plugin:registered` events). **In-heap, same-process, event-bus
  coupled — not an isolation boundary.**
- Deps (program `package.json`): `@ai-sdk/openai-compatible`, `@huggingface/transformers`,
  full **libp2p** stack (`libp2p` + autonat/bootstrap/circuit-relay-v2/identify/kad-dht/mdns/
  noise/tcp/websockets/yamux), `@noble/{ed25519,hashes}`, `@prisma/client`, `alasql`,
  `cozo-node`, `isolated-vm`, `quickjs-emscripten`, `pino`, `ulid/ulidx`, `zod@^4`,
  `micromark/mdast` (GFM markdown), + Tauri note (scaffolded 2026-08-10).
- `tests/arch/`: `arch-invariants`, `layer-dependency`, `api-contract`, `boundary-cdp`,
  `dual-db-boundary`, `store-contract-parity`, `code-quality` — the monolith **knows**
  it has layering rules, enforced in-process.

**Architecture shape (one sentence):** a Bun+TypeScript monolith where 30+ engine
families talk through a `CapabilityEventBus` + `service-container` DI + `module-registry`
+ `ProviderRegistry` singleton, persist through dual-Prisma (201 models) + Cozo, serve
through 40+ Express-style routers + WS, automate browsers through CDP + chrome-governor +
fleet + 19 stealth engines, parse NL through an NLCL engine family, and ship a Next.js +
Tauri product around it.

## 3. Legacy enhanced: measured delta vs program

- `src/`: **1051 files** (+97 vs program). `tests/`: **506 files** (+3). Same `package.json`
  name/version/scripts/deps (one lockfile `bun.lock` 178 KB identical size class).
- **Only two dirs differ at `src/` top level** (verified `Compare-Object`):
  `src/plugin-kernel/` (new) + `src/intel/` (new). Everything else same names.
- `src/plugin-kernel/`: `adapter/impl/{openai-compatible/{adapter,error-mapper,manifest,
  stream-parser},simulator/adapter}`, `bootstrap/orchestrator`, `core/{invariants,types-new}`,
  `event-bus/in-memory`, `plugin-manager/{certifier,contract,install-router,manager}`,
  `sandbox/runner` + `airgap/error-tracker/metrics`. Per
  `plugin-manager/manager.ts` header: `TrustedPluginManager` wraps legacy
  `PluginManagerImpl`, adds 12-attack-vector `certifier.ts` + SHA-256 integrity +
  checksum-gated `discover()/install()`, but **delegates registration back into the same
  `PluginManagerImpl` + `CapabilityEventBus`** ("best-effort", "lazy-loaded to avoid the
  globalThis hack"). Verdict from code: **a certification wrapper around the same in-heap
  registry — still same-process, still event-bus-coupled, not a compartment boundary.**
  Harvestable: certifier vectors, manifest hash shape, adapter impls (openai-compatible +
  simulator incl. `stream-parser.ts`). Not harvestable as architecture.
- `src/intel/`: `auth/auth-gate`, `canvas/canvas-ws`, `cleanup/{deprecated-events,
  unused-exports}`, `domain/slave-state-store`, `executor/{cdp-error-classifier,cdp-types,
  chrome-instance-profile,content-blocks,fleet-limiter,port-reaper,slave-read/slave-write}`,
  `identity/user-identity`, `integration/flag-registry`, `nlcl/` (**40+ files**:
  `budget-engine, catalog, command-registry, confirmation-store, context-binder,
  dialogue-session-store, embedding-classifier, embedding-minilm, entity-resolution,
  fuzzy-matcher/resolver, help-resolver, intent-resolver/router, layered-resolver,
  llm-slave-resolver, nl-interpret/parser, nlcl-engine/otel, parameter-extraction,
  pattern-match, prerouter, response-interpreter, semantic-resolver, text-normalizer,
  tfidf(+provider), types, workflow-synthesis-resolver` + `categories/` 17 files
  `ai/app/automation/browser/builder/canvas/channel/conversation/email/file/llm/memory/
  opencode/provider-cap/session/system/workflow` + `executors/` 11 files), `resource/
  {gpu-allocator,pressure-feed}`, `router/`, `routing/{default-router,strategies}`,
  `runtime/ts-supervisor`. Verdict from code: **a second NLCL + executor + routing stack
  beside `src/engines/nlcl/`, not a replacement** — the 30-file NLCL problem is now a
  ~70-file NLCL problem across two trees. Harvest rule: deterministic resolvers/lexicons
  as data, probabilistic/LLM tails as PROBABILISTIC-only consumers, never as parse-path code.
- Also new at top: `wiki/`, `swarmvault.config.json`, `swarmvault.schema.md`, `CLAUDE.md`;
  `AGENTS.md` shrinks 56 KB → 16 KB. These are **docs/ops deltas, not product deltas**.
- Net: **enhanced = program + an unfinished in-process plugin wrapper + a second
  intelligence stack.** Neither addition changes the coupling lines (EventBus/DI/Prisma
  singletons remain). For Omega purposes both legacies are **one mine with two entrances**:
  harvest algorithms/data/fixtures, never the wiring.

## 4. Omega core: measured facts (the canvas)

- `package.json`: `vivim-omega@0.1.0`, ESM, workspaces
  `contracts, host, shim, platform, plugins/*, packs/*, surfaces/*, examples/*, tooling,
  sdk, testkit`. **One runtime dep: `zod@^4.6.2`.** Scripts: `omega:{setup,test,test:host,
  test:plugins,test:surfaces,quick,gate,decisions,questions,bench,status,demo,portrait,host}`.
  Requires **Bun ≥ 1.3.14** (toolchain), but prod tree is runtime-neutral by gate.
- `host/src/`: 8 files, **911 LOC** (gated ≤1100): `boot/ports/recipe/canon/worker/
  recovery/main/index`. Does 4 things only: verify (recipe sig → manifest sigs →
  content hashes), spawn (one `worker_threads` compartment per plugin, shared-nothing),
  route (`PortRouter` + `callAsRootStream` chunk relay + `waitActive`), enforce
  (host-side token check, `host.compartment.terminate@1`, journal append). Policy lives
  in manifests + LAW_POLICY data, never in host.
- `contracts/src/`: 17 `.ts` files, ~1141 lines, **zero runtime**: `port, manifest,
  recipe, lifecycle, lang, vocabulary, computation, control, provider, variation, agent,
  outcome, parser, chat, surface, consent, index`. Already contains the migration
  vocabulary: `ProviderClass` (SIMULATOR/API_NATIVE/BROWSER_MEDIATED), `RealizationStatus`,
  `ComputationKind` (DETERMINISTIC/PROBABILISTIC/HUMAN), `Variation`, `Outcome<T>`,
  `ParserPin` (data, NOT routable), `ChatConversation/Message/StreamRef`,
  `surfaceOpMeta`, `PrincipalKind`.
- `shim/src/index.ts`: `definePlugin` + port client + `meta.emit` (strict-seq) +
  `probe/probeStat` + `sleepSync`. The only thing compartments may import.
- `platform/src/`: the **only OS-aware module** (`omegaTmp/resolveDataDir/ownerOnly`),
  enforced by `os-surface` gate. Every legacy OS touch (Chrome lifecycle, FS, chmod,
  tunnel, p2p) must go here.
- `plugins/` (21 dirs, 161 files): spine `vivim-law/vault/run`; intelligence
  `vivim-mind/nlcl/nlcl-pure/director/agent/providers/credentials/chat`;
  providers `provider-llm` (simulator+live, `plugin.json` declares `chat.complete@1`
  READ, live leg owner-machine-only behind `credential.use`), `provider-email-file`
  (`message.receive@1`), `provider-browser` (CDP attach, sessions-by-reference);
  discovery `discovery-{perception,observation,inference,mapping,verification,healing}`;
  `law-stub` (Ω0 stand-in). Example manifest (`provider-llm/plugin.json`): id/version/
  entry/contributions(contract+kind+id+version+risk)/dependencies/capabilities
  (requested+justification)/runtime(tier+budget)/contentHash — **hash-pinned, capability-
  justified, budget-declared**.
- `packs/`: `domain-email` (the only pack — SCHEMA+CONTRACT+POLICY+TEST bundle pattern).
- `surfaces/` (32 files): `cli`, `daemon`+`daemon-client`, `mcp`, `web` (console service).
- `compositions/` (16 specs, frozen): `agent, browser, chat, console, credentials, demo,
  discovery, discovery-mind, email, healing, law, llm, notes, run, spine, vault.json`.
  Example `chat.json`: 5 entries (law phase 0; vault/director/llm/chat phase 1), each with
  `grant{capabilities[],contracts[]}` + `config{dataDir:"${TMP}/omega-chat/vault-data"}`.
  Grants are opt-in; forgetting one fails closed (correct direction).
- `docs/VAULT-NAMESPACES.md`: 13 ns rows (`email, automation, nlcl, discovery, agent,
  behavior, decision, providers, probe, law, resolve, control, chat`), each with owner/
  object-ids/writers/retention. `chat` row: `conv:*` latest-wins + `msg:*` append-only
  `{id,conversationId,role,seq,content,providerId?,realizationRef?,streamRef?,createdAt}`,
  retention OPEN with trigger "message latency or compaction pressure in benchmarks",
  `CHAT_HISTORY_CAP` fail-closed. `providers` row: `realization:<archetype>:<provider>`
  latest-wins, written by verify/healing, citing `discovery` promotion events.
- `sdk/` (14 files): typed client/sign/stream/validate; `testkit/` (12): mirrors+harnesses;
  `tooling/` (27): builder/gates/bench/demo/portrait/watchdog/ci; `examples/` (17):
  `plugin-echo/counter/notes`; `fixtures/` (11).
- `build/status.json` (claims at `cc4f6f4`, waves Ω0–Ω13 "built"): host 999/1000 at that
  stamp; current tree host **911/1100** after D-365 budget raise + D-372 extraction.
  Gate: `omega:quick` (structural, seconds) + `omega:gate` (full, minutes, writes
  `build/status.json` + `gates.log`). Laws B1–B5 + Decision Contract + 16-spec freeze +
  `bun-surface`/`os-surface`/`import-surface` + streaming discipline + fail-closed.
- Working tree is **dirty** (21 modified files across workflows, contracts, docs, host,
  platform, examples) — D-372 Phases 3-5 + macOS lane still open. Wave0 must not assume
  a clean tree; first act is `omega:quick` green on a clean clone + pin the base SHA.

## 5. What "stale" means concretely

The prior `00-ASSESSMENT/01-BASE-DELTA.md` through `05-GAP-REGISTRY.md`, `10-WAVE0/*`,
`20-WAVES/*` describe an Omega base by test-count/host-LOC/landed-decision numbers and
a 7-upgrade Wave0. Those numbers were derived from a base this assessment did not
re-derive (no gate was run here), and they assume legacy file counts (163K lines, 2,398
files, 16 providers, 30-file NLCL, 200 Prisma models) this file replaces with the disk
measurements above (954/1051 src files, 503/506 test files, 201 Prisma models, 13+8
provider manifests, ~70 NLCL files across two trees, 19 stealth engines, 40+ routers).
**Keep the old docs as history. Build from this file.**

## 6. Harvest index (code-grounded, replaces triage prose until re-verified)

| Legacy thing (path) | Shape in legacy | Omega destination (existing) | Harvest as |
|---|---|---|---|
| Provider manifests (`seeds/providers/manifests.ts` + 8 JSON) | DB-seeded singleton rows | `vivim-providers` ns `providers` realization rows + `compositions/*.json` grants | DATA (manifest fields → realization rows; selectors/endpoints → fixtures) |
| Provider runtime (`engines/provider-{mux,registrar,discovery,health,selectors}` + `providers/` + `gateway-provider-llm-adapter`) | singleton registry + mux | per-provider plugin (`provider-*`) behind `ProviderClass` + verify PROMOTE | ALGORITHM (routing/health logic re-expressed as realization handlers + discovery verify) |
| Browser automation (`engines/{browser-automation,chrome,cdp-*,live-capture,selector-*,humanized-*,stream-*,harness-*}` + `intel/executor/` CDP fleet + `mcp/browser-*`) | CDP + governor + fleet + stealth | `provider-browser` sessions-by-reference + `platform/` launch lifecycle + law forbidden overlay | ALGORITHM + FIXTURES (mechanics as handlers; sessions as recorded fixtures, never live net) |
| Stealth (`engines/stealth/` 19 files) | in-process spoof engines | law-policy rows + per-file admit/refuse bar | SELECTIVE DATA/POLICY (file-by-file, bulk import refused) |
| NLCL (`engines/nlcl/` + `engines/command-language/` + `intel/nlcl/` 40+ files) | resolvers + categories + executors, LLM in path | `vivim-nlcl`(+`-pure`) rules/lexicon data (ns `nlcl`) + `vivim-director` `resolve.classify` | SPLIT (deterministic → data; `llm-slave/semantic` → PROBABILISTIC tail only) |
| Capabilities (`engines/capability-*`, `capability-bootstrap-generated.ts` 11 KB, `seeds/capabilities/`, `taxonomy/`, `intent-templates/`) | taxonomy + bootstrap + EventBus | `vivim-agent` seed data (ns `agent/behavior/decision/control`) + contracts | DATA (shapes as seed rows; EventBus/binder/composer REMOVE) |
| Conversations/memory (`engines/conversation-manager/history-sync/context-assembly/memory-engine/knowledge-*`, `src/domain/`, `seeds/user/`) | manager + Prisma models | `vivim-chat` ns `chat` (`conv:*`/`msg:*` + index) + `vivim-credentials` | SHAPED ROWS (never Prisma imports; per-conversation index + retention rule first) |
| Scheduler/onboarding/run (`engines/{scheduler,automation,harness,workflow-*,lifecycle,task-history}`, `seeds/automation/`) | ticks + cron sidecars | `vivim-run` + `vivim-director` tick data (ns `automation`) | DATA (rules/ledgers as rows; cron sidecars REMOVE) |
| Tunnel/p2p (`engines/{tunnel-client,tunnel-orchestrator,p2p-node}`, libp2p deps) | sockets + DHT + relay | `platform/` sockets + explicit forbidden policy + budgets | PATTERN (no code lifted; re-express behind platform + law) |
| Observability/resilience (`engines/{observability,telemetry-*,otel-sink,metrics,health-*,resilience,reliability,retry-*,eviction-*,compaction-*}`) | loggers + sinks | `vivim-mind` query + journal + bench walls + watchdog | PATTERN (query set + SLO walls; no sidecar) |
| Surfaces (`src/server/` 40+ routers, `src/cli/`, `src/mcp/`, `frontend/` Next.js, `src-tauri/`) | monolith server + full frontend + Rust shell | `surfaces/{cli,daemon,mcp,web}` + frontend-as-HTTP-client pointer | CONTRACT (HTTP surface parity first; rebuild only on proven mismatch) |
| Plugin wiring (`engines/plugin-system`, `server/{module-registry,service-container,plugin-router}`, `engines/events/`, `plugin-kernel/`, `intel/routing/`) | EventBus/DI/ModuleRegistry/ProviderRegistry | Port Protocol + Recipe grants + host-side caps | REMOVE (never port; vendor algorithms with provenance headers only) |
| Storage runtime (`src/storage/`, Prisma 201 models, Cozo, snapshots) | Prisma + Cozo + cache | `vivim-vault` ns-KV + sqlite + FTS + changelog + compaction | REMOVE runtime, HARVEST shapes (one ns row per domain, same-commit) |

**Non-goals confirmed by code:** no host growth (B5), no legacy writes (mines are
read-only; snapshot SHAs above are the pins), no bulk port (one provider/one capability
proves the pattern), no new contract without writer+reader+test same commit, no
composition sprawl (16-spec freeze holds until a generator replaces it mechanically).
