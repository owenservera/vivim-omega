# D-446 — Adaptation Constitution is law (Omega-16 port, paper D-448)

## Status

RATIFIED

## Context

- Substrate deterministic and sovereign; intelligence arrives as black boxes that reroute attention, pour context, drift budgets, bury tiles — smart-feature creep kills by a thousand cuts (paper `D-448` §1: helpful override, opaque prefetch, black-box drift).
- Requires Ω-1..Ω-8 green (tree D-431..438): adaptation proposes to something real; without substrate it proposes to air.
- Invariant: adaptation proposes, substrate disposes; prediction/anticipation/personalization allowed, execution/decision/concealment never; probabilistic guest, deterministic center.
- Tree collision noted: tree D-446 here vs paper Ω-14 specId `D-446` — distinct per the two-numbering rule (this tree row ports paper `D-448`).

Blocks: none

## Options

| Criterion | (a) Core meta-law port (tooling + tests, zero host LOC) | (b) Per-plugin smart bypasses | (c) Defer |
|---|---|---|---|
| Propose-only airlock | Yes — direct exec refuses, pins veto absolutely | Neural net decides | Magic 8-ball |
| No hidden tensors | Yes — weights/scores/outputs are badged rows | Ephemeral cache | Unauditable |
| Fallback immortal | Yes — no-fallback registration refused | Halt on ML death | Fragile |
| Kill-switch | Yes — global + scoped dumb, immediate | Always-smart | No retreat |

## Decision

**Decision:** (a) — port paper `D-448` as tree D-446: adaptation-realization classification, propose-dispose interface, transparency mandate, mandatory deterministic fallbacks, global/scoped dumb-switch, revocation to baselines.

## Consequences

- Harder: every smart feature registers with fallback or refuses; hunches become rows; vetoes stay absolute; survival never needs intelligence.
- Easier: ML ships without soul-risk; intuition auditable; #351–360 governable; clockwork available on demand.
- Revisit: fallback-equivalence proofs; per-layer dumb granularity; adaptation admission checklist; intuition-UX strings.

## Evidence

- F-ADAPT.1 (propose-dispose) — pinned-moving proposal refuses at substrate, logs conflict, proposer adapts.
- F-ADAPT.2 (global-dumb) — dumb-switch drops ML prefetch/arrange, deterministic rules carry 100% function.
- F-ADAPT.3 (transparency-audit) — prefetchwhy folds scorer, confidence, inputs; zero black boxes.
- F-ADAPT.4 (fallback-survival) — ML death freezes state, manual substrate continues, nothing vanishes, nothing throws.
- Spec: paper `D-448` (9-16 spec lines 3472–3614); requires Ω-1..Ω-8.
- Gate: owner-directed ratification per owner directive 1 — isolated falsifier greens pre-flip; full-suite crash recorded as pre-existing environmental exception (Windows soak/MCP, baseline-documented); status.json stays last-green-carried.
- Landed in `9e45326` (PROPOSED commit; ratified on owner directive 1).

## Index

summary: Port Omega-16 propose-dispose transparency fallback dumb-switch from paper D-448
rationale: Adaptation proposes; substrate disposes; principal holds kill-switch
class: evidence
