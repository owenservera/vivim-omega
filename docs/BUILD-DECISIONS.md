# Ω Build Decisions (D-2xx build track)

Append-only register for the fresh-tree build. Phrasings mirror the program repo's D-register
conventions (PROPOSED → RATIFIED; supersede, never edit).

| ID | Decision | Status | Rationale |
|---|---|---|---|
| **D-210** | Adopt VIVIM-Ω (D2) as the architecture of record and **build the core fresh**: a standalone tree (`vivim/vivim-omega`), not shaped by, strangling, or harvesting the legacy vivim code during the build. Legacy backfill happens **after** the core exists ("we will later backend into the new core"). | **RATIFIED** (owner directive, verbatim: *"I want you to first fully build the new core system with everything you outlined in vivim omega - we should not shape the perfect end state with the old vivim code - we will later backend into the new core"*) | Supersedes D3's D-201 (build inside vivim-final-enhanced) and defers D-204 (harvest-during-build) to the backfill phase. The old repos are frozen mines, untouched (fresh-tree guard gate). |
| **D-211** | Zero-runtime-dependency core: compartments via `node:worker_threads`, storage via `bun:sqlite` (WAL+FTS5, probed green), signatures via `node:crypto` ed25519, hashes sha256. The @noble pin (D-206) is amended to node:crypto — same algorithm class, no dependency. | RATIFIED (verified in-sandbox 2026-09) | Available today, boring, honest. Algorithm fields remain pinned in the Recipe (`hashAlgo`) so upgrades are declared changes. |
| **D-212** | Waves Ω0–Ω9 per D3 `03-WAVE-SPECS`, each ending in a **booted composition** with green gates (`bun run omega:gate`), benchmarks in `BENCHMARKS.md`, and status in `build/status.json`. Tests are gate evidence (TEST is contribution kind #9) — the sandbox no-test rule applies to the Next.js review console, not to the Ω product itself. | RATIFIED (owner authorized the build of "everything you outlined") | D-205's existence-proof law, carried into the fresh tree. |
| **D-213** | Ω0 composition includes a `law-stub` plugin occupying the `vivim.law` slot (id `vivim.law`, allow-all policy) so the boot-order law is real from wave 0. Ω1 replaces it via a recipe re-compile + atomic pin swap — the first live demonstration of the amendment protocol. | PROPOSED | Keeps the µhost honest (phase 0 = vivim.law is enforced, not special-cased) without requiring the full law plugin in wave 0. |
| **D-214** | Anti-loss: local commits per task on branch `omega`, wave tags `omega/w0`…, `git bundle` + tarball mirrored to the sandbox `download/` area via the console's `/api/download` route. No pushes (sandbox has no credentials — §4.7 law). | RATIFIED (standing law) | D-208 generalized to the fresh tree. |
| **D-215** | Self-knowledge is a PLUGIN (`vivim.mind`, Ω10), not µhost code. The µhost stays untouched (≤1,000 LOC law). The mind derives the WorldModel from law.registry + vault + composition-config projections; it writes nothing, so a wrong self-model is falsifiable against intact evidence. | RATIFIED (owner: "if there are any core primitives needed in the core ensure you properly segment - if pure plugins is best that's ok") | Pure plugins is best: every concern in the directive (self-knowledge, NLP, reprogramming, feedback) is a swappable plugin; the core stays boring. |
| **D-216** | NCLL (Natural Command Language Layer) is an upgradable ENGINE plugin (`vivim.nlcl`, Ω11) wrapping a dependency-free pure package (`plugins/vivim-nlcl/pure` published as `@vivim/omega-nlcl-pure`, zero imports). Determinism law N1: same (text, WorldModel, version) ⇒ same interpretation — in compartment, surface server, and browser. The LLM is never in the parse path (edge-only, opt-in, marked probabilistic — law N2). | RATIFIED (owner: "the deterministic NLP and nclc as a core upgradable plugin") | One signed unit to hot-swap the whole language engine (recipe re-compile + atomic pin swap); same bytes for keystroke-latency browser feedback and authoritative server parse. |
| **D-217** | Symbol families: 17 core-primitive command families (`/ @ # ! ? + - & $ * % ∆ ✓ = ^ ~ →`) are LEXER primitives (code); their NL expansions are lexicon data. Canonical symbolic forms are first-class IR output shown beside every NL parse (gap-narrowing feedback). | RATIFIED (owner directive, verbatim symbol list) | Families are grammar (stable), meanings are data (upgradable/teachable). |
| **D-218** | Language frames (verbs/slots/prepositions per op) live as data in nlcl-pure keyed by op id; taught words live in vault ns `nlcl` (survive plugin swaps). Post-v1 path: a `lang` contribution kind owned by packs — a contract-grammar change = amendment-class event, deferred. | RATIFIED (design recommendation) | Keeps v1 contract surface frozen; upgrades stay atomic; the amendment path is documented, not improvised. |
| **D-219** | Reprogrammability (Ω12 `vivim.director`) is DATA ONLY: rules (vault ns `automation`), teachings (ns `nlcl`), consents (law ops). A deterministic 500 ms tick loop matches vault state against rules and executes actions through the port under principal `vivim.director` — the law still gates rule actions (one consent at rule-creation). No code is generated, ever. | RATIFIED (owner: "BACKEND IS FULLY REPROGRAMMABLE BY NON TECHNICAL USERS USING NATURAL LANGUAGE") | NL → data records → deterministic engines. Journaled, Merkle-chained, revertable. |
| **D-220** | `surfaces/web` (Ω13): a third surface (after cli/mcp) serving the WorldModel + authoritative interpret/execute/consent over HTTP and a live journal/world stream over socket.io (path `/`). The browser replicates the WorldModel and runs nlcl-pure locally for instant typing feedback; execution is always the server's parse. | RATIFIED (owner: "as the user types there is instant interpreted feedback") | Local parse ≈ server parse because they share the same pure bytes (N1). |
| **D-221** | The sandbox runs the web surface via `mini-services/omega-engine` (launcher, port 3031, `bun --hot` — the sandbox service-folder law). The implementation lives in `surfaces/web` as a first-class surface; the launcher is a thin entry that imports it. | RATIFIED (sandbox constraint + architecture) | The Ω repo keeps its surface; the sandbox keeps its deployment law. |
| **D-222** | Email pack + provider v0.2.0: add CONTRACT `message.receive@1` (READ — ingestion is vault-internal; the simulator semantics are documented) + provider handler + frames. Enables the "when Peter messages me" rule loop. | RATIFIED (owner example, verbatim: "send this to Peter" / rules) | Contract addition on the pack is append-only (0.1.0→0.2.0); existing 1.x deps unaffected. |
| **D-300** | CDP transport is Bun-native `WebSocket` + `fetch`; no CDP client library is adopted. | **RATIFIED** | Continues D-211's zero-runtime-dependency law. |
| **D-301** | `provider-io-browser` v1 supports `attach` mode only; `launch` mode is deferred. | **RATIFIED** | Avoids per-OS process-lifecycle and packaging work before the observation/action substrate itself is proven. |
| **D-302** | Provider activation uses recipe recompile + atomic pin swap (D-213's mechanism); no new router/selector plugin is built. | **RATIFIED** | Mechanism already exists and is tested; a parallel selection layer would create two ways to change what serves an op. |
| **D-303** | Field-level mapping confidence (§9.2) is a candidate-ranking signal only; it never gates promotion. | **RATIFIED** | Preserves Ω8's "promotion is proof, not confidence" invariant — stricter than the DB track's own rule; this build keeps the stricter one. |
| **D-304** | Monkey-patch instrumentation (§7.2.1) is rebuilt natively, not adopted from any third-party interceptor library. | **RATIFIED** | Sidesteps license review entirely by not vendoring code. |
| **D-305** | Protocol/stream inference is reimplemented as small pure functions (§9.3), not ported from any GPL-licensed library. | **RATIFIED** | Algorithm concepts reimplemented natively; no code copied; keeps zero-new-dependency law intact. |
| **D-306** | `ProviderClass` (§5.1) is typed to admit a fourth member (e.g., `INTELLIGENCE_HARNESS`) without a breaking change, but that member is not added and no harness provider is built in Ω14–16. | **RATIFIED** | The DB track operates a real, working LLM-serving harness that will eventually need a provider class of its own; reserving the type now avoids a breaking change later, without pulling DB-track scope into this wave. |
| **D-307** | The realization-status vocabulary (Part 3) and the confidence-vs-proof distinction (constraint 6, Part 2) are published as a standalone versioned contract (Gate G0) before Ω14.0, and become the vocabulary the DB track's control-plane conforms to going forward. | **RATIFIED** | Two independently-evolving systems converging on the same "provider realization" concept risk hardening incompatible vocabularies; Omega's version is the reference. |
| **D-308** | `Variation` contract added (`contracts/src/variation.ts`); mapping gains additive `discovery.variations@1` grouping candidates by contractId (best-pick `discovery.map@1` output untouched). Multiple variations may be co-PROMOTED; routing preference is `vivim.law` policy. | **RATIFIED** | Toolbar button and keyboard shortcut for the same op are concurrently-valid realizations, not competing candidates; alternatives graduate to status-carrying entities. |
| **D-309** | Ω Control Plane v0: `AgentIdentity`/`BehaviorContract`/`DecisionRecord` (`contracts/src/agent.ts`) + `plugins/vivim-agent` (spawn/describe/propose/promote/rollback/decision). Reuses `LifecycleState`, vendored scope algebra, `Outcome<T>` from day one — no new token-minting or state-vocabulary machinery. | **RATIFIED** | Smallest working vertical slice for a persistent, permissioned, lineage-tracked agent, gated the same way every other Ω wave is gated. |
| **D-310** | `BehaviorContract.forbiddenActions` enforced by a per-principal overlay table inside `vivim.law` (`law.forbidden.set@1`), checked first in `law.check@1`. Capability scope and forbidden policy stay separate checks through the one gate. | **RATIFIED** | Reusing the one gate every mutation already passes through avoids a second host-invisible choke point; memory-only overlay, recipe amendment stays the durable path. |
| **D-311** | Perception node-kind union widened by exactly what `Variation.channel` demands (`menu`, `menu-item`), each backed by the `webmail-menu` capture fixture — not by a speculative full ontology. | **RATIFIED** | An unfixtured node kind is an unverified claim about application shape; evidence-before-authority applies to the ontology itself. |
| **D-312** | `Outcome<T>` (`contracts/src/outcome.ts`) used from day one on every new op for expected-but-negative results; thrown exceptions reserved for genuinely unexpected failures. | **RATIFIED** | Extends the existing `REFUSED`/`BUDGET` refusal-register pattern into plugin-level ops consistently, shaped by actual return needs rather than retrofitted. |
| **D-313** | Agent runtime fork: acting loop (B1a-first, evidence over debate) vs descriptive records. Detail: `docs/decisions/D-313-agent-runtime.md`. | **RATIFIED** · directive | Machinery that looks alive is the worst outcome; B1a produces evidence for the fork. |
| **D-314** | DB-track conformance: human-attested checklist now, fixture test only when both sides publish. Detail: `docs/decisions/D-314-db-conformance.md`. | **RATIFIED** · directive | Checklist costs nothing; shared packages couple irreversibly — sequence strength with evidence availability. |
| **D-315** | Live-agent quarantine semantics (finish vs halt vs migrate): process rule decided (blocker on shipping past fixture-replay), semantic TBD during B1a. Detail: `docs/decisions/D-315-quarantine-semantics.md`. | **RATIFIED** | Writing the code with the question live reveals which answer the invariant needs. |
| **D-316** | Composition flagship vs N first-class compositions — DECIDED (b): N first-class compositions, no flagship. The D-376 net holds all 16 specs to identical risk-parity + matrix conformance with no free passes, and has produced its findings without a reference composition (grant variance is handled by D-385's DRIFT_ALLOWLIST rows, each naming its own D-pointer, never a privileged spec). The matrix's named risk for (a) — premature centralization of harness-serving compositions — is confirmed by the tree's composition inventory (product surface, pipeline slices, single-purpose harnesses are different-in-kind; discovery-mind's parser carriers are dormant by design). Flagship status may still be declared later by a NEW record if a product need (migration-cutover blessing) demands one. Detail: `docs/decisions/D-316-composition-flagship.md`. | **RATIFIED** · directive | Owner-delegated per the standing directive (D-367 fast-path); the record's own revisit precondition met — the conformance net shipped as evidence-class D-376 with its seeded-drift falsifier green; 2026-09-18 independent recommendation §6 concurs; board reaches zero open items. Landing SHA `e7de299` (decision text + archive pointer), flip same-day on gate green. |
| **D-317** | Test-lane sharding trigger is test count (~700), not wall clock. Detail: `docs/decisions/D-317-test-budget.md`. | **RATIFIED** · directive | Count is machine-independent; clocks punish slow boxes and reward test deletion. |
| **D-318** | Phase A placement: extend `discovery-mind.json` (verification already lives there) over a new `providers.json`. Detail: `docs/decisions/D-318-phase-a-placement.md`. | **RATIFIED** | Reversal cost is one moved entry; confirmation is low-ceremony. |
| **D-319** | G1 spike scope: A1 boot canary + A2 get + A3 verify-writes + A5-minimal registry + A6 namespaces now; A4 healing writes follow. Detail: `docs/decisions/D-319-g1-spike.md`. | **RATIFIED** | End-to-end proof this gate cycle beats a bigger unreviewable wave; merge blocked on the namespaces doc. |
| **D-320** | CI: ubuntu-required gate + windows-informational test; fresh-tree skips loudly (never green, never red) with no siblings. Detail: `docs/decisions/D-320-ci-workflow.md`. | **RATIFIED** | Independent confirmation without punishing 2-core runners or clean clones. |
| **D-321** | Bun `resourceLimits` unenforced (verified 217MB in a 32MB-capped worker): document now in `host/src/worker.ts`, watchdog later. Detail: `docs/decisions/D-321-resource-limits.md`. | **RATIFIED** | Missing protection, not false claim — but the pitch invites the assumption. |
| **D-322** | Daemon warm path (Upgrade A): per-vault TCP daemon + thin CLI with cold fallback; falsifier is daemon-side RTT ≤ 5ms plus separately-reported cold wall. Detail: `docs/decisions/D-322-daemon-warm-path.md`. | **RATIFIED** | Unix sockets unverified on Windows; per-call spawn floor is unremovable — measure the split honestly. |
| **D-323** | Computation routing (kind enum + 3 director ops + scorecard). Detail: `docs/decisions/D-323-computation-routing.md`. | **RATIFIED** | V2.3: route by computation kind before exposing discovery; needs its own writer (ns `resolve`). |
| **D-324** | Provenance linkage (epistemicStatus + buildDecisionRef). Detail: `docs/decisions/D-324-provenance-linkage.md`. | **RATIFIED** | Ships with V2.3; three epistemic values, VERIFIED reserved for probe-backed writes. |
| **D-325** | Forbidden-overlay durability: vault-journaled overlay (ns `law`), fail-closed set with rollback, boot reload once the vault is queryable, `law.forbidden.reload@1`. + ConsentTable-asymmetry note (doc-only). Detail: `docs/decisions/D-325-forbidden-durability.md`. | **RATIFIED** | FIRST — fail-open → fail-closed before any new actor exists to walk through the open door. |
| **D-326** | G1 closure: healing writes DEGRADED/TESTING to ns `providers`. | **RATIFIED** | Makes D-307 conformance non-vacuous before agents consume it. |
| **D-327** | B1a: one agent acts once (realization→execution wiring under `agent:<id>`). Blocked on D-315 decided first. | **RATIFIED** | Proves execution exists before V2.3 routes to it. |
| **D-328** | Control-plane v0 slice: D-328a read/orient (`control.bootstrap` + `control.describe` + `agent.snapshot`, ships first) then D-328b act/evolve (`agent.delegate` + `evolution.*` aliases, may slip alone). Detail: `docs/decisions/D-328-control-plane.md`. | **RATIFIED** | Exposes discovery only after something worth discovering exists. |
| **D-329** | Warm worker pool (isolate pooling + adversarial state-bleed suite as ship-blocker). | **RATIFIED** | V2.5 latency B; touches B2 directly. |
| **D-330** | Content-hash cache (daemon-side, hit mints tokens only). | **RATIFIED** | V2.5 latency C; must be unobservable to a correct caller. |
| **D-331** | Lazy activation (bootPhase 0 eager, rest spawn-on-first-call, `dormant` vs `degraded`). | **RATIFIED** | V2.5 latency D. |
| **D-332** | Conformance-net check: new exported `contracts/` type with zero call sites flagged. | **RATIFIED** | Backstops the plan's top risk mechanically (V2.5 hygiene). |
| **D-333** | Omega as the sole migration substrate (M-series governance); `kernel-plugins`/`end-state` retired as architecture target, inventory salvaged. Detail: `docs/decisions/D-333-migration-substrate.md`. | **RATIFIED** | D-210 backfill phase; one target must win or the scaffolded migration tooling executes the retired plan. |
| **D-334** | Fresh-tree pin follows the published legacy snapshot (`afebe00`; program stays `4a5eb84`). Detail: `docs/decisions/D-334-freshtree-pin.md`. | **RATIFIED** | A stale pin fails the gate for reasons unrelated to any change under review. |
| **D-335** | M2: conversation storage lives in the vault as-is for the pilot, retention explicitly open. Detail: `docs/decisions/D-335-storage-scale.md`. | **RATIFIED** | A storage answer is needed before the pilot's first write; a ceiling in writing beats premature machinery. |
| **D-336** | M3: human principal `user:<id>` parallel to `agent:<id>`, reusing forbidden + consent. Detail: `docs/decisions/D-336-human-principal.md`. | **RATIFIED** | The principal shape must exist before multi-user pressure, or sessions bolt on outside the law gate. |
| **D-337** | M4: deterministic-first resolution, ambiguous-only PROBABILISTIC fallback. Detail: `docs/decisions/D-337-resolution-rule.md`. | **RATIFIED** | Same problem as legacy, opposite philosophy — needs a ruling before the pilot, not during it. |
| **D-338** | M0: `BROWSER_MEDIATED` realization scope + authority bar (law-reviewed, fixture falsifier). Detail: `docs/decisions/D-338-browser-realization.md`. | **RATIFIED** | Largest trust surface so far; scope and bar in writing before code or the pilot fakes its own mechanism. |
| **D-339** | M1: additive streaming primitive with cold fallback (`echo.stream@1` falsifier first). Detail: `docs/decisions/D-339-streaming-primitive.md`. | **RATIFIED** | No partial-result shape exists in the Port Protocol; additive-with-fallback or the host budget pays. |
| **D-350** | Re-land the self-view: `mind.portrait@1` (5th engine contribution + `port:vault.verify@1`) + `omega:portrait` emitter + tolerant posture parser (honest `[]` until PRINCIPLES.md returns). Detail: `docs/decisions/D-350-self-portrait.md`. | **RATIFIED** | Owner directive 2026-09-14; landed + gated in 33cfdee (624/624). First-lineage numbers reproduce exactly — the re-landing is faithful. |
| **D-351** | Risk parity: one truth for the gate. `LAW_POLICY_V1` 1.1.0 (exact rows `vault.roundtrip@1`→EXTERNAL_MUTATION, `providers.session.start@1`→MUTATION; `notes.*`→`note.*` repair) + the fail-closed parity net over every shipped composition (manifest risk === policy classification). Detail: `docs/decisions/D-351-risk-parity.md`. | **RATIFIED** | L1 from the owner's 360 review (`upload/L1-DUAL-RISK-SOURCE.md`): the manifest decides whether the gate fires, the policy decides what it says, nothing reconciled them — a manifest-declared EXTERNAL_MUTATION op executed without consent (probe-proven) and another would demand consent it never declared. Needed before the M0/M7 re-land wave. |
| **D-352** | M1 re-land: the additive streaming primitive (`StreamChunk {streamId,seq,data,final}` + `checkStreamSeq`; host chunk relay with sink-or-drop cold fallback + `callAsRootStream` the only host surface; shim `meta.emit` strict-seq close-once with fail-closed producer; sdk `streamRootCall` generator; testkit differential mirror; `echo.stream@1` falsifier on a real boot). Detail: `docs/decisions/D-352-streaming-reland.md`. | **RATIFIED** | D-339's pending engineering, re-landed after reset 2 per the map's begin order (M1+M3 from D-352). 651/651 green; the D-339 falsifier now proves ordered delivery, cold fallback, BUDGET mid-stream, and DEGRADED on producer violations. |
| **D-353** | M3 re-land: the human principal — `PrincipalKind`/`principalKind` (pure, TOTAL; unknown strings stay legal composition principals) + `law.describe@1` (kind + forbidden{ops,persisted} + principal-narrowed active consents + generation) + `ConsentTable.listFor`; law 0.2.0; granted in all 13 law-carrying compositions; law-stub stand-in with real classification and honest empty tables. Detail: `docs/decisions/D-353-human-principal-reland.md`. | **RATIFIED** | D-336's pending engineering, re-landed after reset 2 per the map's begin order. `user:<id>` is attributable, forbid-able, consent-able, inspectable with zero new tables — M2's chat pilot names a human principal from D-354. |
| **D-354** | M13: the isolation tier for M7 parser execution — the existing B2 plugin compartment, parsers = plugin-grade trust BY CONSTRUCTION of M7 (the parser-signing key is plugin-publisher-grade power, stated out loud), zero-capability pure transforms, deadline-BUDGET containment, D-321's memory exposure cited; WASM + M17 taxonomy reserved for the isolation-taxonomy ruling. Detail: `docs/decisions/D-354-parser-isolation.md`. | **RATIFIED** | Decision-first: the map's M13 trigger demanded the ruling BEFORE parser code; the containment suite is part of the wave. |
| **D-355** | M7 re-land: the parser as governed, version-pinned data — `CONTRIBUTION_KINDS += "parser"` (NOT routable), `contracts/src/parser.ts` (ParsedChunk/ParserPin/parserContributionId/asParserPin/pinMatches/buildChunkEnvelope), `ProviderRealization.parserPins?` written by discovery.verify@1's provider input (total-validated, dup-checked). Detail: `docs/decisions/D-355-parser-governance.md`. | **RATIFIED** | D-348's engineering re-landed after reset 2; the M0 wave's first code record, landed after D-354 by design. |
| **D-356** | XC-2 write side + M12: the credentials spine — `credential.put@1` (MUTATION exact row + require-consent RULE; policy 1.2.0; sim-synthetic REFERENCES only, material refused fail-closed), `credential.use@1` (READ, capability-dead-by-construction), `credential.redact@1` (REDACTION_POLICY_V1 versioned policy data; redact-before-append; JSON-structure-preserving). Detail: `docs/decisions/D-356-credentials-spine.md`. | **RATIFIED** | Built against SURFACES.md's credential law per 360 addendum A1; the M0 wave's credential first writer. |
| **D-357** | M0 re-land: the BROWSER_MEDIATED realization — sessions by reference (redact-before-vault), four fail-closed bars (fence / attached / PROMOTED / pin), the replay riding D-352's chunk shape, `compositions/browser.json`, and the G0 evidence-ref seam fix (perception/observation refs carry `{ns,id,rev}` + derived casRef). Detail: `docs/decisions/D-357-browser-realization.md`. | **RATIFIED** | D-338's pending engineering, re-landed after reset 2; the D-338 ship-blocker falsifier now green on a real boot of the shipped composition. |
| **D-358** | M2: chat storage in the vault — `contracts/src/chat.ts` (ChatConversation/ChatMessage/ChatStreamRef + id grammar + narrowers), `plugins/vivim-chat` the FIRST WRITER of ns `chat` (chat.open@1 MUTATION / chat.append@1 MUTATION with writer-assigned seq + realizationRef/streamRef provenance / chat.history@1 READ bounded + sibling-proof), `chat.complete@1`'s additive line-chunk emission (the M1→M2 persistence hook, contract unchanged), `compositions/chat.json`, ns `chat` row same-commit, LAW_POLICY_V1 1.3.0 exact rows. Detail: `docs/decisions/D-358-chat-storage.md`. | **RATIFIED** | The chat pilot wave (map begin order item 1, from D-358): D-335's storage ruling implemented — vault as-is, `chat` namespace row in the same commit, retention open with the named revisit trigger. |
| **D-359** | M4: chat resolution wired through the SHARED derivation — `contracts/src/surface.ts` (`surfaceOpMeta` + `capabilityNamesFromRouted`; the MCP tool-generation rule as one contracts definition, A2), surfaces/mcp refactored to import it, `chat.resolve@1` (READ) in vivim-chat: deterministic half (exact command → rule branch, ledgered as a ns-resolve ResolveDecision row by vivim.chat) + ambiguous half via the SHARED `resolve.classify@1` (the director's own row; no parallel resolution logic). Resolution ≠ execution. Detail: `docs/decisions/D-359-chat-resolution.md`. | **RATIFIED** | D-337's binding consequence implemented one-for-one; the derivation's consumer set extends (MCP + chat) without a third binding. |
| **D-360** | The consumption watchdog (D-321's specified follow-up): `tooling/watchdog` attaches to a booted router, polls active compartments, probes raw workers (shim `probe`→`probeStat`; a wedged loop CANNOT answer), terminates violators via `host.compartment.terminate@1` as root; two-signal enforcement — N consecutive unanswered probes (unresponsive) or N consecutive over-budget heap samples against the manifest's declared `runtime.budget.memMB`; evictions journal; minimal host seam (`CompartmentHandle.worker`, `PortRouter.compartmentWorker`); README law #3 downgraded to "coupling, not exhaustion" with the fresh Linux numbers. Detail: `docs/decisions/D-360-watchdog.md`. | **RATIFIED** · evidence | The external review's §1 (highest severity); falsifiers adversarial 13/14 landed green BEFORE ratification per D-364 cooling-off; resourceLimits re-verified NOT ENFORCED on Linux (130MB in a 32MB cap). |
| **D-361** | Bun surface containment: the production tree is runtime-neutral — `plugins/vivim-vault/src/db.ts` is the ONLY `bun:sqlite` importer (`openDatabase` + type re-export; changelog/roundtrip/tests ride it), sleep is Atomics-based `sleepSync` (shim export + host-local), `Bun.file`/`Bun.listen`/`Bun.spawn` swapped to `node:fs`/`node:net`/`node:child_process` (host main, cli/mcp/web boots, daemon, daemon-client, decisions checker, status emitter); dev/test toolchain stays Bun with the requirement stated and pinned (≥ 1.3.14) in README's Run section; the gate gains the `bun-surface` stage enforcing the inventory mechanically. Detail: `docs/decisions/D-361-bun-surface-containment.md`. | **RATIFIED** · evidence | The external review's §2; the declared-adapter falsifier is the gate stage itself; node canary (D-362) proves the core is runtime-neutral. |
| **D-362** | Gate claims reproduce mechanically: `tooling/ci/verify-status.ts` re-runs `omega:gate` and fails unless the COMMITTED `build/status.json` structural claims reproduce (stage flags, host LOC, test counts, waves, benchmarks) — modulo `generatedAt` and runner-shape fields; wired as the required `gate-reproduce` CI job alongside a required `node-canary` job (canon suite under Node 24); gate-status refresh commits must cite the landing commit + command. Detail: `docs/decisions/D-362-gate-reproducibility.md`. | **RATIFIED** · evidence | The external review's §5; self-attestation becomes self-checking; the committed status.json is now a CI-verified claim. |
| **D-363** | Boot readiness rides the `ready` message: `PortRouter.waitActive(id, timeout)` (immediate-resolve if active, pending-resolvers woken by the router's ready case, crash path rejects) replaces boot.ts's 25ms pollers — a delivery-mechanism swap with unchanged bounds; demo boot measured 27ms → 10ms. Detail: `docs/decisions/D-363-boot-event-wiring.md`. | **RATIFIED** · evidence | The external review's §3 (low-risk cleanup); falsifier = measured boot drop + all boot-path suites green. |
| **D-364** | Decision-log classes + cooling-off + consolidation: index rows from D-360 on carry `· evidence`/`· directive` class tags (checker-enforced; pre-360 grandfathered untagged); B1–B4 evidence-class decisions require the named falsifier IN the record before ratification, plus a second gate run after (directive rows keep same-day ratification, honestly labeled); consolidation pass every ~30 ratifications refreshes `docs/decisions/CURRENT-INVARIANTS.md` (pass #1 ships with this row). Detail: `docs/decisions/D-364-decision-process.md`. | **RATIFIED** · directive | The external review's §4; process debt paid down without rewriting the audit trail; the checker rule + contract tests are the mechanical teeth. |
| **D-365** | Host budget 1100 + freeze (sole-owner boring-host law): gate enforces 1100, README + CURRENT-INVARIANTS state the freeze (no new host surface without removing old surface in the same commit). Detail: `docs/decisions/D-365-host-budget.md`. | **RATIFIED** · directive | 999/1000 pressure is fragility, not discipline; the number was arbitrary, the principle is what matters. |
| **D-366** | Watchdog hardening: heap-spoof note (self-reported probeStat — only unresponsive is non-spoofable), two-path kill (unresponsive→fast hard-kill 500ms cap via terminateFast + host {fast} flag; memory→graceful), budget audit (budgetStatus + requireBudget/onDefaultBudget journal). Detail: `docs/decisions/D-366-watchdog-hardening.md`. | **RATIFIED** · evidence | D-360 follow-up; falsifiers 13/14 stay green (reasons additive); new unit tests for fast flag + budgetStatus. |
| **D-367** | Decision-process simplification: directive fast-path default (commit + gate green = approved), invariants-first reading, stale-board discipline (backlog rows older than two wave-sets get decided or re-affirmed). Detail: `docs/decisions/D-367-decision-simplification.md`. | **RATIFIED** · directive | Solo-owner speed without losing B1–B4 safety or the append-only audit trail. |
| **D-368** | Test lanes + quick gate (D-317 trigger reached at 733): lane scripts omega:test:host/plugins/surfaces + omega:quick (structural stages only, no status write). Detail: `docs/decisions/D-368-test-lanes.md`. | **RATIFIED** · directive | Count trigger hit; inner loop needs a fast lane, merge gate unchanged. |
| **D-369** | Small hardening batch: chat id comment {1,64} correction, stream maxBufferedChunks 1000 flood guard, verify-status fail-fast + /1100 log line, daemon socket-error buffer drop, BENCHMARKS owner-wave reservation. Detail: `docs/decisions/D-369-small-hardening.md`. | **RATIFIED** · directive | Owner line-review nits, all fail-closed and green-preserving. |
| **D-370** | Node-boot proof + composition freeze (plan): freeze at 16 specs (no new spec without deleting/generating one), 1-day Node-boot spike protocol with pre-authorized either-outcome. Detail: `docs/decisions/D-370-node-boot-plan.md`. | **RATIFIED** · directive | D-361 neutrality claim needs a real boot log or an honest commit to Bun. |
| **D-371** | Windows takeover conversion: best-effort chmod in ensureVault, .gitattributes LF normalization, docs/WINDOWS.md runbook, omega:setup alias, README pointer; /tmp paths kept by explicit proven-green decision. Detail: `docs/decisions/D-371-windows-takeover.md`. | **RATIFIED** · directive | Team works on Windows from here; one unsafe path + three gaps, minimal gate risk. |
| **D-372** | Multi-OS platform seam: one codebase every OS with zero per-OS branches — new platform package holding the only OS-aware code, fail-closed os-surface gate stage, portable ${TMP} dataDir spelling, lane-by-lane test migration, Windows lane to required. Concept: `docs/MULTI-OS-DESIGN.md`. Detail: `docs/decisions/D-372-multi-os-platform-seam.md`. | **RATIFIED** · directive | Fence the six touchpoints so supporting a new OS means adding a CI lane, never editing product code. |
| **D-373** | DB-agnostic storage core: the vault driver lane — `plugins/vivim-vault/src/drivers/` (bun-sqlite default + node-sqlite lazily loaded) behind one structural `SqliteDriver` seam, `contracts/src/storage.ts` wire vocabulary (ns/rev/refs preserved + health()), bun-surface allowlist rewritten from one-file to the driver lane, cross-runtime conformance parity suite (identical workload digest on bun:sqlite under Bun vs node:sqlite under Node 24); Postgres = step 2 with its own falsifier. Detail: `docs/decisions/D-373-db-agnostic-storage-core.md`. | **RATIFIED** · evidence | W0-10 foundation tier (D-375 arbitration of DRAFT-002); the D-361 one-importer promise becomes mechanically proven instead of documented. |
| **D-374** | Polyglot process tier: `RuntimeTier` widens additively (wasm forward-declared only per D-354 reserve), `PluginManifest.runtime.process` additive, `platformSpawn()` in the D-372 seam, `ProcessBroker` inside vivim-run exposed as `run.process.call@1` (MUTATION exact row), pools declared only in signed composition config (unknown pool REFUSED), ndjson stdio shims (python + node) in `shims/polyglot/`, malformed IPC bounded → BUDGET never hang; host 0 LOC. Detail: `docs/decisions/D-374-polyglot-process-tier.md`. | **RATIFIED** · evidence | W0-10 foundation tier (D-375); the D-360/D-321 exhaustion-containment residual gains its sanctioned process-per-compartment escape hatch. |
| **D-375** | Foundation drafts arbitration: DRAFT-002 is the landing spec (its falsifier set governs D-373/D-374); DRAFT-003 adopted selectively (driver health(), pool shape) with named deferrals (wasm, S3, streaming ops, telemetry, OS-enforced limits); MASTER deferred in full (TEE/synthesis/autonomic need their own records + falsifiers); drafts recorded verbatim in `docs/migration/10-WAVE0/received/`. Detail: `docs/decisions/D-375-foundation-drafts-arbitration.md`. | **RATIFIED** · directive | Owner delivered three escalating drafts + delegated ratification for this wave; nothing dropped silently — the deferral triggers are the record. |
| **D-376** | W0-2 conformance net completed: the `compositions` gate stage gains check 6 (D-351 risk parity at the GATE layer — every routed op with declared non-READ risk classifies identically in LAW_POLICY_V1; formerly test-only) and check 7 (matrix conformance — every shipped spec regenerates byte-identical from `compositions/_matrix.json`, first-diff line named); `_`-prefixed files skipped as non-specs; seeded-drift falsifier (`conformance-drift-seed.ts`, copied to 40-EVIDENCE/W0/) proves green baseline + named diagnostics on a lost grant and an undeclared risk. Detail: `docs/decisions/D-376-conformance-net.md`. | **RATIFIED** · evidence | W0-2's two remaining gaps closed before Wave1 touches compositions; the net now fails closed on both drift shapes. |
| **D-377** | W0-1 authoring path: `compositions/_matrix.json` is the source of truth for all 16 specs; `tooling/generate/format.ts` is THE canonical JSON formatter (parse∘render idempotent); `omega:generate` (tooling/generate/generate.ts) emits `composition [--check]` (byte-identical regeneration / gate-mode compare), `plugin <name>` (manifest+package+pure core+shim entry+test+authoring checklist) and `pack <domain>` (SCHEMA+CONTRACT+POLICY+TEST skeleton); 16 specs canonicalized once (JSON-content identical, run.json note verbatim); D-370 freeze untouched — the boot falsifier runs in gitignored scratch. Detail: `docs/decisions/D-377-authoring-generator.md`. | **RATIFIED** · evidence | Falsifiers green: 16/16 byte-identical, drift named with first-diff line, scaffolded plugin passes sdk validation and boots first-try on a real host (real workflow: scaffold → bun install → boot → restore). |
| **D-378** | W0-3 vault index + probe: vivim-chat maintains a per-conversation index row (`idx_<hex>`, latest-wins, `{conversationId, count, entries}` bounded by CHAT_HISTORY_CAP) in the same append critical section; history reads ≤ CAP rows via the index (legacy scan = fallback; corrupt index refuses fail-closed); cap/seq checks read the index; probe (`omega:probe`) measures 1K/10K/100K real appends + indexed history + cap refusal + verify; probe-driven storage fixes: `changelog_ns_id_rev` index (next-rev was a full scan per append) + `ftsInsert` replacing `ftsUpsert`'s per-append full-FTS DELETE; retention windows per ns declared with numbers (enforcement Wave3). Detail: `docs/decisions/D-378-vault-index-probe.md`. | **RATIFIED** · evidence | Append solo p50 flat vs scale; indexed history p50/p99 flat; cap refusal fail-closed on the indexed path; vault.verify green; driver parity digests unchanged. |
| **D-379** | W0-4 sharing boundary (Phase 1): single `user:<id>` attribution; `chat.history@1` gains optional `principal` — a cross-principal read returns the REFUSED verdict envelope (policy verdict as data, vivim-run pattern; never DEGRADED) AND appends a `refusal_*` ledger row (ns chat, meta.type principal-refusal: owner, caller, op, at); absent principal keeps the trusted-reader path; workspace/session/key shapes land as attributed rows, never shared objects; sharing reopens only by a new decision record. Detail: `docs/decisions/D-379-sharing-boundary.md`. | **RATIFIED** · evidence | Falsifier pinned in the chat pilot on a real boot: user:mallory's read of user:ada's conversation refuses + the ledger row names both principals; owner + absent-principal paths unchanged. |
| **D-380** | W0-5 provider realization bar (ruling now, code Wave2): (1) byte-identical fixture rule — post-parser canonical extraction identical modulo a per-provider volatile allowlist declared as data; (2) launch mode is Wave2-only through `platform/`, extending the D-357 bar to LAUNCHED processes; (3) per-file stealth admission — each of the 19 legacy stealth files admitted with a law reason or refused with one, never bulk; (4) API_NATIVE-first — BROWSER_MEDIATED only where automation is the product, per-provider call named. Detail: `docs/decisions/D-380-provider-bar.md`. | **RATIFIED** · directive | The only trust expansion the migration may make, barred before any realization is harvested. |
| **D-381** | W0-6 parser landing bar: parsers land as DATA (ParserPin rows + version-pinned signed blobs + fixtures) executed only in the D-354 isolation tier with deadline-BUDGET — never routable ops, never onParse-style hot-path hooks; Wave0 owns the rule, Wave1 runs the proof (legacy SSE stream + import fixture through pins on a real boot, parserPins surviving the round trip). Detail: `docs/decisions/D-381-parser-bar.md`. | **RATIFIED** · directive | The legacy onParse architecture is dead on arrival; healers/CDP mechanics enter only as pin data under the D-380 admission discipline. |
| **D-382** | W0-7 observability spine spec: the spine is vivim.mind + vault query + journal stream (no metrics sidecar, no legacy telemetry engines — patterns only); query set = per-conversation history, per-realization status, per-decision resolve trail, per-eviction journal; walls named now (BOOT, RTT, SPAWN, APPEND-LATENCY, EVICTION), SLO objectives in Wave6; every wave's falsifier cites the same walls. Detail: `docs/decisions/D-382-observability-spine.md`. | **RATIFIED** · directive | D-378's probe is the first APPEND-LATENCY instrument; BENCHMARKS extends per wave. |
| **D-383** | W0-8 surface contract first: pointer default — the existing Next.js frontend points at Omega surfaces over HTTP (no Prisma/routes ported); MCP tool generation via the shared surfaceOpMeta derivation; Tauri defers to Wave5 on proven shape mismatch only; console hardens to authenticated-user over user:<id> + credentials + consent (the D-379 fence is the mechanism); CLI is the sanctioned stand-in until Wave5 wires the pointer. Detail: `docs/decisions/D-383-surface-pointer.md`. | **RATIFIED** · directive | Waves 1–4 code against a stable surface shape; the Wave5 falsifier text is fixed now. |
| **D-384** | Assessment remediation (two external gap assessments, owner-directed): (1) scoped `host.tokens.revoke@1` is SCOPED — per-record revocation state (the ConsentTable pattern), generation bump reserved for revoke-all, ownership checked before revocation state; (2) `atomicWrite` unique tmp names (casPut pattern) + failed-rename unlink + `cleanupStaleSwap` sweeps `recipe.pinned.tmp*`; (3) `contentHashDir` rejects symlinks fail-closed (B1 hash covers exactly the tree's bytes); (4) `classifyRisk` overlapping prefixes resolve by specificity, order-independent (D-351 exact-outranks-prefix unchanged); (5) consent principal binding — `law.consent.grant@1` refuses non-root cross-principal callers, web `/api/consent` drops the client `principal` (id-only ceremony); (6) `daemon.json` writes mode 0o600 + `ownerOnly()` seam (the root-key treatment, applied to the bearer secret); (7) provider-llm live leg documents the sim-synthetic spine contract honestly; (8) KNOWN-LIMITS L-11 posture + L-17 director tick caps. Detail: `docs/decisions/D-384-assessment-remediation.md`. | **RATIFIED** · directive | 10 new falsifiers green (revoke symmetry, forgery refusal through real law, symlink throw, tmp hygiene, specificity, daemon 0600, web principal-ignored via journal audit); host 999→1039/1100 B5 held; suite 797/797. |
| **D-385** | W1 harvest infra: (1) `discovery-mind.json` carries `provider.llm` + `vivim.chat` as PARSER-contribution carriers with EMPTY grants (signed manifests for the W1 falsifier's compile ceremony, never spawned — parser contributions register no op, D-355); cross-composition grant variance allowlisted with this pointer; (2) the governed parser-pin fence — `discovery.parser-registry@1` POLICY contribution is the CLOSED set of five shipped parser pins, `discovery.verify@1` refuses any pin outside it BEFORE any vault write (fail-closed genealogy: no promotion event, no realization row for ungoverned pins; unpinned runs unchanged); (3) T-06 identification filed mechanically from the pinned mine (14 boundaries / 22 files verified at `vivim-final-program@4a5eb84`). Detail: `docs/decisions/D-385-w1-parser-carriers-fence.md`. | **RATIFIED** · directive | W1 falsifier green on the real discovery-mind boot (evidence rows, promotions carrying pins, consent-ceremony roundtrip survival, mind-queryable, fence refusal pre-write); fixture pipeline verified (4 recorded rows: hash + shape + determinism); matrix byte-identity green 16/16; gate GREEN ×2 (PROPOSED eb1c573 + post-flip), suite 830/830, host 1039/1100 flat. |
| **D-386** | OS-enforced containment probe (the L-15 revisit trigger, elevated to a pre-autonomy gate by the 2026-09-18 independent recommendation §7): `platform/src/containment.ts` + `omega:containment` — Linux cgroup v2 single-OS experiment; detects the hierarchy (writability tested by mkdir, never inferred), places a REAL child in a probe subgroup under `memory.max` (+ swap 0 when delegable), orders a 4× over-cap allocation, reads kernel-side accounting (`memory.peak`, `memory.events` oom_kill) the child cannot spoof; verdicts enforced/advisory/unavailable with enforcement claimed ONLY from measurements that demonstrate bounding; self-reported numbers carried as data, never verdict input; zero production behavior change. GATE CONDITION: B1b and any Wave2 LAUNCHED provider require verdict enforced on the target OS or a recorded owner acceptance. Detail: `docs/decisions/D-386-containment-probe.md`. | **RATIFIED** · evidence | Falsifier green at landing 9ae6662 (gate 847/847, host 1039/1100 flat): parsers refuse-to-null, verdict truth table (incl. swap-unbounded downgrade + slack boundary), detection coherence, end-to-end honesty invariant, real-kernel case where writable (honest skip elsewhere; this container: cgroup v1 ro → unavailable, archived in 40-EVIDENCE/OWNER/); second gate run post-flip per D-364 cooling-off. |
| **D-387** | Hot-path performance remediation (2026-09-18 external performance review, 9 findings verified against the tree): (1) console world-poll — socket-gated (zero watchers → zero vault reads), bodiless version-check path (`mind.snapshot@1 {includeBodies:false}`), full payload only on version change; new `vault.getmany@1` READ contract (latest-rev-per-id batch, missing-is-data, corruption-still-throws, 512-distinct bound) and the mind's evidence reads parallelized through `allEvidence()` (every rejection observed — lost shutdown races never surface unhandled); (2) compaction FTS delete batched via temp-table `ftsDeleteMany` — one scan per pass, same transaction (measured 782→18 ms at 2k candidates); (4) verify set-based (one NOT-EXISTS probe for dangling revisions, DISTINCT-cid CAS stats; lowest-seq divergence reporting preserved); (5) journalHistory bounded backward chunk read (flat cost vs the journal's lifetime size); (6) async tail pump with in-flight guard; (9) classifyRisk precomputed WeakMap index, differential-proven ≡ the per-call filter+sort (D-351/D-384 pinned). MEASURED-AND-REVERTED: the #3 liveRefs json_extract prefilter (lost to plain JS parse); DEFERRED with named triggers: #7 batch-append fsync boundary (trigger: bulk-import wave requirement), #8 test-only compile-path hash cache (trigger: Budget-watch suite wall-time line). Detail: `docs/decisions/D-387-perf-remediation.md`. | **RATIFIED** · evidence | Falsifiers green at landing e2f756b (24: vault 10, web 8, policy 6); compaction 782.1→17.97 ms @2k candidates, journal history 8.56→1.08 ms @5.7 MB (flat), window 201→2 port hops, liveRefs prefilter measured-and-reverted (3.8–7.1 vs 3.2–3.9 ms); gate GREEN ×2 (871/871, host 1039/1100 FLAT) per D-364 cooling-off; matrix byte-identity 16/16. |
| **D-388** | Performance remediation round 2 (2026-09-18 second external performance review, findings verified at 0815d8c): (§2.1) the director tick's read phase batched — every new/changed candidate fetched in ONE `vault.getmany@1` and every fired-ledger check in ONE more, replacing up to 2×200 sequential per-row gets per 500 ms tick (measured 403 → 5 read hops at the 200-row cap); missing-is-data, no-refire, at-least-once, and partial-report semantics preserved row-for-row; rule loading stays lazy per-row ON PURPOSE (skip-not-fatal survives; the batch-all-fail alternative ruled out); director requests `port:vault.getmany@1`, console+chat re-granted via matrix regen byte-identical. (§2.2) pool burst observability — `omega:bench` burst case (8 concurrent vs poolSize 2) + `daemon start --pool-size` knob + documented rule of thumb; MEASURED CORRECTION of the review's narrative: sync refill serves every burst checkout from parked stock (8/8 hits, 0 fallbacks) — the real burst cost is the inline thread spawn per checkout (~11–13 ms); `coldFallbacks` stays the worse-signal detector. (§2.3) interim L-1 measure: `runtime.budget.intervalMs` per-compartment watchdog cadence (additive SDK field; pure policy `dueForSample`/`watchdogTickInterval`; undeclared compartments keep the 250 ms global default) — D-386 gate condition held unchanged. Dispositions without code: L-4 backfill stays W3-owned (wave boundary law), L-13 SLOs F-3-owned, L-8 deadline keeps its trigger (exposure narrowed), L-14/L-17 stand; D-387 Evidence repaired with its landing citation (checker-demanded, D-384 precedent, no substance edit). Detail: `docs/decisions/D-388-perf-remediation-2.md`. | **RATIFIED** · evidence | Falsifiers named before ratification (15: tick 8, pool 2, watchdog 5) green at landing 3ccfe07; harness case F 403→5 read hops; bench burst 8/8 hits 0 fallbacks ~87–106 ms wall; gate GREEN ×2 per D-364 cooling-off (886/886, host 1039/1100 FLAT); compositions byte-identity 16/16. |
| **D-389** | Intent mechanism (four phases): new plugin `vivim.intent` over the existing law/vault/director ports — durable intent objects (ns `intent`/`intent-plan`), plan templates with multi-step DAG resolution, JSON-pointer payload projection (depth ≤ 4) applied at step execution, cancellation with consent-gated compensation evidence (compensation WRITES evidence, never implies rollback); kernel-side dynamic router and host-minted per-intent tokens ruled out on B1/B5 grounds; full saga/rollback engine and the projection consumer deferred with named triggers (`plan:<type>@2`); plan/saga wire exports allowlisted under the D-332 reservation discipline. Detail: `docs/decisions/D-389-intent-mechanism.md`. | **PROPOSED** · evidence | Falsifier tests green (intent.test.ts 17 pass: submit/resolve/step-execute with delegation evidence/cancel-with-compensation/status; intent-phase3-4.test.ts: projection, depth bound, compensation consent gate); landing commits `6975c4c`…`cb022ad` resolvable on `main`; workspace wiring repaired during setup (plugin package.json + pre-compile contentHash per the sdk schema law). |
| **D-390** | History-reset evidence reconciliation: the 2026-09-18 re-base of `main` onto the adopted vivim-omega-latest tree (`f780d06` → `6d6a3ad`) stranded the pre-reset landing SHAs in the Evidence of records D-313…D-388 (~20, `git cat-file` unresolvable, substance untouched); authorizes ONE mechanical, additive Evidence re-citation line per affected record — adoption commit `f780d06` attests the pre-reset landing state, every original SHA and word preserved, no other section changes, checker law unchanged — the checker-demanded evidence-repair path per the D-384/D-388 precedent. Detail: `docs/decisions/D-390-history-reset-evidence-recitation.md`. | **PROPOSED** · directive | Mechanical pass output archived with the setup commit; falsifier: `omega:decisions` green + decisions checker self-host test green on the same tree; first green gate run including the re-citation commit is the ratification citation. |
