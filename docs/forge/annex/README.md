# docs/forge/annex/ — the working-set annex

Two-line status discipline: **annex files are working material — versioned,
citable, never law.** The vision itself is law-tier
(`docs/forge/OMEGA-ENDSTATE-VISION.md`, D-407/D-408); records live in
`docs/decisions/`; precedence stays with the records and the gate. Everything
here is analysis, pre-analysis, and program-side working material that travels
with the repo so it can be cited by path instead of by memory.

| file | what it is |
|---|---|
| `OMEGA-VISION-ATOMS.md` | the 713-line vision atomized into 94 end-state claims, coverage-mapped to falsifiers/refusals/capability rows |
| `OMEGA-CORE-STRUCTURAL-ANALYSIS.md` | the structural pass — core-vs-tactical grading by retrofit-cost triggers; the S1/S2/S3 shortlist; the tactical map |
| `OMEGA-CONSOLIDATION-INTEGRATION.md` | the Consolidated Core reconciliation — supersession ledger, the D-389 collision de-collided as OD-9, the OD-1…OD-9 open-decision register |
| `OMEGA-PROGRAM-ACCELERATION.md` | the pre-Wave-1 program review — the B1–B10 bottleneck register, accelerator designs A1–A12, the chosen build order |
| `OPERATIONAL-EFFICIENCY-AUDIT.md` | the course-correction turn's hand-held ledger — every read sized (≈418 KB transport for ≈6 KB decision payload), B-register reconciled (B11/B12 registered), the native-tooling register A13–A17 + the harness worklog law, the credit line and restraint register — **A13–A17 LANDED as D-422 (ratified 2026-09-20): omega:brief, gate --failures-only/--stage, omega:docscan (report-only), omega:entry, the ledger home** |
| `OMEGA-CORE-FIRST-RESEQUENCE.md` | D-410's material — the core-omega-ready milestone, the parked-plugin register, the post-core sequence, falsifiers of the ordering |
| `OD-1-CAPTURE-VS-READ-FORK.md` | the lift-ready fork file — decided (a) split-plugin by the owner 2026-09-20, lifted as D-409; implementation parked per D-410 |
| `S3-EVIDENCE-STORE-FORK.md` | **lifted as D-416 (RATIFIED 2026-09-20)** — the evidence-store choicepoint, pre-analyzed lift-ready: the owner's call took (a) fold-into-vault; the audit-chain persistence point landed with it (law.audit.drain@1, ns audit); the volume clock re-measured at lift: 7,145 rows / 65 journals / one full-gate run, unchanged from the D-413 measurement |
| `PARKED-STATE.md` | the park record (2026-09-20) and its lift — historical; documents the omega ↔ vision separation era and the restore path used |

Provenance: authored 2026-09-20 in the external working set (`vivim-vision/`)
during the pre-decisioning park; landed verbatim at re-entry in D-410's round.
Internal references to `vivim-vision/` denote that working-set mirror (kept
outside the repo for delivery). From re-entry on, new working material — fork
files for S3 and any later fork, OD-2+ pre-analyses, program reviews — is
authored **here**, in the annex, so nothing diverges from the tree again.
