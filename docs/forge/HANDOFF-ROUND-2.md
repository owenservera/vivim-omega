# Handoff — Round 2 (Wave 0 close-out)

Written per the round packet §9. Branch `wave0-omega-forge`. Round 2 entry commit:
`dd9ba28`. This round lands **no code** — it closes the end-of-round protocol that
Round 1 left open and re-proves the falsifier on a fresh boot.

## What landed

**Round 1** (`0763556` PROPOSED → `d7bf70d` Ratify D-403..D-406, two Linux full
greens 1056/0 ×2 → `dd9ba28` status refresh):

| D-item | Artifacts |
|---|---|
| D0 | AGENTS.md drift fixes; decision records D-403/D-404/D-405/D-406 in `docs/decisions/` |
| D1 | `tooling/gates/anvil.ts` (budget 860, surface 45) + `tooling/gates/test/anvil.test.ts`; gate stage 1b |
| D2 | `contracts/src/manifest.ts` generality block; `sdk/src/schema.ts` + `sdk/src/validate.ts` four validators; `sdk/test/fixtures/generality/` (8 pinned) + `sdk/test/generality.test.ts` |
| D3 | `packs/builder/` — plugin.json (24 `forge.*` contract contributions), `src/schemas.ts` (FORGE_OP_CATALOG), contract/policy docs, 14 fixtures + `schema.test.ts` |
| D4 | `plugins/forge-author/` (generated files + AUTHORED regions + spec/self.json); `compositions/forge-author.json` via `_matrix.json` (18th spec); `plugins/forge-author/test/{happy/refusal}/`; `docs/VAULT-NAMESPACES.md` ns `proposal` row |
| D5 | `tooling/gates/forge-surface.ts` five checks + `tooling/gates/test/forge-surface.test.ts`; gate stage 5d |
| D6 | `fixtures/mines/synthetic-v0/` — 42 files, rootHash-pinned MANIFEST.json, own suite 10/10 |

**Round 2** (this commit): protocol close-out only — `docs/forge/wave0-evidence.md`
gained the packet §7 round ledger + the §8 acceptance checklist (11/11 `[x]`);
this handoff file was created; the falsifier was re-verified green on this boot
**before** the docs commit (numbers below).

## Falsifier status

**GREEN** — re-verified on a real boot, fresh process, entry commit `dd9ba28`:

```
bun test plugins/forge-author/test/happy/self-host.test.ts \
       plugins/forge-author/test/refusal/init-refusals.test.ts --timeout 60000
# 31/31 pass
bun test tooling/gates/test/forge-surface.test.ts tooling/gates/test/anvil.test.ts --timeout 60000
# 23/23 pass
bun test sdk/test/generality.test.ts packs/builder/test/schema.test.ts --timeout 60000
# 39/39 pass
```

`forge.author.init@1` reproduces `plugins/forge-author/` byte-identical outside
`// AUTHORED` regions (rules 1–6, double-run identical), and the deliberate
hand-edit red case fails with the named diff `SPEC_BYTE_DRIFT`. Full-gate
standing: two Linux greens 1056/0 ×2 at ratification, plus the post-ratification
green behind `dd9ba28`; the status-refresh commit on top of this one re-runs the
full gate on this tree.

## Hand-fix tally and backlog

**0** hand-edits to generated bytes across both rounds (raw material of the
tally's five bootstrap re-pins is logged in the evidence file §Hand-fix tally).
Backlog: `docs/forge/BACKLOG.md` —

- seven forge plugins declared in wire, unimplemented by design;
- `forge.mine.capture@1` against `fixtures/mines/synthetic-v0/` (**Wave 1**);
- GEN_SPECULATIVE_STALE caller/wave tracking (report-only until Wave 1);
- forge.mine capture-vs-READ partition split (first Wave 1 decision).

## Ambiguities recorded, defaults used

1. **Round numbering** — the packet names input bundle `_1`, output `_2`.
   Default: count bundles, not context windows; Round 1 produced `_1`'s content,
   this session is Round 2. (Recorded in the evidence round ledger.)
2. **Exit-commit self-hash** — a commit cannot embed its own hash; the exit SHA
   is cited in the status-refresh commit message and is the tip of bundle
   `vivim-omega-wave0-omega-forge_2.bundle`.

## Next round's exact entry point

**Wave 1, first D-item: `forge.mine.capture@1` against the pinned synthetic
second mine.**

1. First command (falsifier baseline must still be green):
   `bun test plugins/forge-author/test/happy/self-host.test.ts --timeout 60000`
   then `bun run omega:quick`.
2. Read `docs/forge/BACKLOG.md` and `fixtures/mines/synthetic-v0/MANIFEST.json`
   (rootHash-pinned, 42 files, `tools/hash.py --check` → 0 bad).
3. Record the deferred capture-vs-READ partition decision (next decision id:
   **D-407**) before any code — the mine's READ-class row vs the capture
   receipt's write path is the seam that was deliberately left open in D-406.
4. Land `plugins/forge-mine/` per the packet §5 grammar (manifest shape law:
   risk on contract contributions, `granularity`, `internalSeams`, generality
   `speculative`, `mine: null`); refusal test required before the happy path is
   considered covered.
